import { CategoryItemsPipe } from './category-items.pipe';

describe('CategoryItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
